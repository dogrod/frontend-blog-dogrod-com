/**
 * RegEx to extract ID from mixed slug like `${slug}-${id}`
 */
export const REG_EXTRACT_ID_FROM_MIXED_SLUG = /[a-zA-Z0-9]+$/

/**
 * RegEx to validate email
 */
export const REG_EMAIL = /^[a-zA-Z0-9_-]+@[a-zA-Z0-9_-]+(\.[a-zA-Z0-9_-]+)+$/
